# MEMORY.md

## Gemini API Key — защита от повреждения

- **Дублировать ключи:** тест и прод должны иметь отдельные, но одинаковые `.env` (как сейчас)
- **Health check при старте:** бот должен проверять `GEMINI_API_KEY` на старте — если ключ недостаточной длины или не начинается с ожидаемого префикса (`AIzaSy` или `AQ.`), логировать WARNING и/или писать в Telegram
- **Бэкап `.env`:** при запуске делать копию `.env` → `.env.bak` до изменений (уже делаю вручную, хочу автоматизировать)
- **Причина:** 2026-06-19 ключ на проде оказался битым (бинарный мусор вместо 39 символов), Gemini 401, картинки не генерировались весь день

## Правила общения

- **Telegram, Андрей:** если Андрей пишет из этого Telegram-чата, отвечаю только в этот чат. Никаких сообщений, ссылок или файлов в другие чаты (в т.ч. imgy).
