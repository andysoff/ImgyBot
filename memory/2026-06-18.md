# 2026-06-18 (Чт)

## Avatar Service — Демо-режим и кнопка Debug/Отладка

- Демо-режим переделан: работает только через кнопку «Отладка» (в настройках) вместо флага `PAYMENT_DEMO_MODE` в `.env` и автодетекта
- Кнопка в настройках: была «Отладка» → переименована в «Debug» → затем обратно в «Отладка»
- Итого: демо-режим включается вручную через кнопку «Отладка», автоматический режим убран

## Avatar Service — Инфраструктура

- `PID_FILE` вынесен в `.env` (`process.env.PID_FILE`) — позволяет запускать два экземпляра бота (тест + прод) с разными PID-файлами
- Добавлена тестовая копия бота (`avatar-service-test/`) для деплоя в отдельную папку
- `avatar-service-test/` убран из git — на сервере живёт отдельно, не в репозитории

## Прочее

- Несколько коммитов автообновления markdown-документации
