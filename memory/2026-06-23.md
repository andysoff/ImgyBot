# 2026-06-23

## Avatar Service — Identity Lock эксперименты

**Проблема:** Gemini не всегда соблюдает черты лица с референсного фото в середине промпта.

**Что делали:**

1. **22:20** — `8c560e2` Debug: полный payload Gemini (без обрезания) — для анализа, что именно уходит в API
2. **22:22** — `5e3660f` Identity lock: новый компактный текст `IDENTITY: facial features MUST match reference photo EXACTLY`, перенесён в конец промпта (Gemini лучше запоминает последние инструкции)
3. **23:04** — `91abf0e` **Revert** identity lock к оригинальной версии — CRITICAL IDENTITY LOCK в середине промпта оказался нерабочим, вернулись к старому варианту

**Итог:** Эксперимент не удался, identity lock откатили. Старый промпт остался без изменений.
