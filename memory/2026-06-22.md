# 2026-06-22 (понедельник)

## Avatar Service — доработки генерации изображений

Серия коммитов в `avatar-service/`, в основном правки `generate-image.js`:

- **Усиление промптов для сохранения лица** — identity anchor в промптах, чтобы Gemini лучше сохранял черты лица (b2b721a)
- **Запрет украшений** — серьги, бижутерия явно запрещены в casual-стилях (03adbce)
- **Temperature 0 → 0.1** — сначала понижена до 0 для стабильности (48b7469), потом поднята до 0.1 для небольшой вариативности (3c358f0)
- **Поворот лица** — по умолчанию сохранять угол поворота как на референсе (8a8e756), затем фикс для `faceTurn='none'` (d60ef71)
- **Логирование** — добавлен лог промпта (a0b6688), увеличено до 1000 символов (6db2deb), добавлено логирование полного payload в Gemini (17be08a — временное, для отладки)
- **Защита от случайного деплоя на прод** — триггер-файл `/tmp/imgy-prod-deploy-ok` (b15df8d, правки `deploy/deploy-prod.sh` и `AGENTS.md`)

Файлы, затронутые сегодня: `generate-image.js`, `deploy/deploy-prod.sh`, `AGENTS.md`.
